namespace CursesMenu
{
    /** 
    * \brief Funkcja kontrolująca działanie programu, wersja pod ncurses.
    * 
    * Funkcja zajmująca się obsługą menu programu
    * zarządza tym co będzie wywoływane.
    * Wykorzystuje ncurses do generacji menu
    */
    void obslugaMenu();
}